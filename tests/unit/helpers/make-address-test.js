import { makeAddress } from 'parking-tracker/helpers/make-address';
import { module, test } from 'qunit';

module('Unit | Helper | make address');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = makeAddress([42]);
  assert.ok(result);
});
